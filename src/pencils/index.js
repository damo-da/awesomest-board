import pencil from './pencils/pencil';

export const getPencilById = (id) => {

  switch(id.toUpperCase()){

    default:{
      return pencil;
    }
  }
};

export const getPencilsData = () =>{
  return [];
};
